schema: year

extends: entity_core

fields:

  year_number:
    type: number
    section: Temporal

  era:
    type: string
    section: Temporal
    optional: true

  canonical_events:
    type: entity_list
    target: event
    optional: true

  simulated_changes:
    type: entity_list
    target: entity_core
    optional: true
