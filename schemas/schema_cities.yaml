# ==================================================
# FULL DIFF
# Gathered collected changes from this chat
# ==================================================


# --------------------------------------------------
# NEW FILE: schemas/schema_cities.yaml
# --------------------------------------------------

schema: cities.yaml

extends: entity_core

fields:

  city_class:
    type: string
    optional: true

  city_role:
    type: string_list
    optional: true

  location_entity:
    type: entity
    target: locations

  parent_location:
    type: entity
    target: locations
    optional: true

  controlling_factions:
    type: entity_list
    target: factions
    optional: true

  associated_institutions:
    type: entity_list
    target: institutions
    optional: true

  population:
    type: dict
    optional: true

  urban_structure:
    type: dict
    optional: true

  city_systems:
    type: dict
    optional: true

  cultural_features:
    type: string_list
    optional: true

  embedded_sites:
    type: string_list
    optional: true
